<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://www.snehalraj.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://www.snehalraj.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-16T16:57:09+00:00</updated><id>https://www.snehalraj.com/feed.xml</id><title type="html">blank</title><subtitle>Snehal Raj, PhD student at Sorbonne University. Quantum Computing, Reinforcement Learning, and Natural Language Processing. </subtitle><entry><title type="html">“Train classical, deploy quantum” requires rethinking generalization</title><link href="https://www.snehalraj.com/blog/2026/train-classical-deploy-quantum/" rel="alternate" type="text/html" title="“Train classical, deploy quantum” requires rethinking generalization"/><published>2026-09-14T04:30:00+00:00</published><updated>2026-09-14T04:30:00+00:00</updated><id>https://www.snehalraj.com/blog/2026/train-classical-deploy-quantum</id><content type="html" xml:base="https://www.snehalraj.com/blog/2026/train-classical-deploy-quantum/"><![CDATA[<p>Our new preprint, written with Natansh Mathur and Alejandro Perdomo-Ortiz at QC Ware, asks a question that the train-classical, deploy-quantum literature has mostly skipped. Once the classical loss has converged, does the quantum circuit generalize? The paper is on <a href="https://arxiv.org/abs/2608.31117">arXiv</a>.</p> <h2 id="the-setup">The setup</h2> <p>A quantum circuit Born machine prepares a parametrized state and samples it in the computational basis. For some circuit families, that sampling is believed to be classically hard. Training such a circuit on hardware is expensive. Each gradient needs many circuit runs, and barren plateaus can flatten the loss landscape.</p> <p>The train-classical, deploy-quantum (TCDQ) strategy avoids this cost. The loss is evaluated and minimized on a classical computer. The quantum device only draws samples after training. This works when the loss depends on quantities that are classically tractable, even when sampling the full distribution is hard. The usual example is the squared maximum mean discrepancy, MMD², which compares the model and the data through their Pauli-Z correlators.</p> <p>Prior work on TCDQ asked two questions. Can the classical loss be optimized? Can a classical surrogate reproduce the deployed model? We ask a third. Once the loss has converged, does the model produce new, valid samples?</p> <h2 id="what-generalization-means-here">What generalization means here</h2> <p>We follow Gili et al. and score a generative model by the samples it produces. Let \(S\) be the valid set and \(T \subset S\) the training set. The model generalizes if its forward KL divergence to the target, \(\mathrm{KL}(p^* \,\|\, q_\theta)\), is small. This divergence is infinite unless the model gives positive probability to every valid string. In deployment we cannot compute it, so we use two sample-based proxies.</p> <ul> <li><strong>Coverage</strong> is the fraction of unseen valid strings, \(S \setminus T\), that appear among \(Q\) samples from the model.</li> <li><strong>Fidelity</strong> is the fraction of samples that are valid.</li> </ul> <p>A model that emits uniform random bits sets a no-learning floor for coverage, \(C_{\mathrm{rand}} = 1-(1-2^{-N})^Q\).</p> <h2 id="moment-losses-cannot-certify-generalization">Moment losses cannot certify generalization</h2> <p>Every deployed TCDQ loss is a moment loss. It depends on the model only through a finite list of \(d\) observable expectations. MMD² with a Gaussian-Hamming kernel is a weighted sum over all Pauli-Z correlators. A fixed-order correlator loss keeps only the correlators up to order \(L\), so it has capacity \(d = \sum_{\ell \le L} \binom{N}{\ell} = O(N^L)\).</p> <p>Our main theorem says that such a loss has exact minimizers with almost no coverage.</p> <p><strong>Theorem.</strong> Let \(p^*\) have full support on a finite valid set \(S\), and let \(\mathcal{L}\) be a moment loss of capacity \(d\) with \(d+1 &lt; \lvert S \rvert\). Then there is an exact global minimizer \(q_{\mathrm{sparse}}\) of the population loss, supported on at most \(d+1\) strings, with</p> \[\mathcal{L}_*(q_{\mathrm{sparse}}) = \mathcal{L}_*(p^*), \qquad \mathrm{KL}(p^* \,\|\, q_{\mathrm{sparse}}) = \infty, \qquad C_\infty(q_{\mathrm{sparse}}) \le \frac{d+1}{\lvert S \rvert}.\] <p>The target and the sparse minimizer give the same loss value, so the loss cannot tell them apart. On the cardinality task, where the valid set holds every \(N\)-bit string of Hamming weight \(N/2\), an order-\(L\) correlator loss admits a minimizer with coverage \(O(N^{L+1/2} / 2^N)\). The proof is the Boolean-cube form of the argument Arora et al. made for GANs with bounded-capacity discriminators.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/tcdq/mmd_kl_demo-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/tcdq/mmd_kl_demo-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/tcdq/mmd_kl_demo-1400.webp"/> <img src="/assets/img/blog/tcdq/mmd_kl_demo.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Two exact minimizers of the same truncated moment loss. The distributions p<sub>good</sub> and p<sub>bad</sub> match every correlator through order L, yet their support coverages are 1 and 0.07. </div> <p>The minimizer is explicit. A linear program at N = 12 and L = 2 finds a distribution that matches every correlator of the uniform target through order 2 on a support of 66 strings. Its truncated loss is about 10⁻²⁸. Its coverage is 0.07 against the target’s 1.00. Its forward KL is infinite.</p> <div class="row mt-3"> <div class="col-sm-8 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/tcdq/scaling_props-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/tcdq/scaling_props-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/tcdq/scaling_props-1400.webp"/> <img src="/assets/img/blog/tcdq/scaling_props.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The low-coverage minimizer scales as the theorem predicts. For N = 8 to 16 at L = 2, a linear program builds a distribution that matches every correlator of the uniform target up to order 2. Realized coverage (red) and the bound from the corollary (gray), on a log scale. </div> <p>The full MMD² is different in principle. Its kernel is characteristic, so the target is the unique minimizer of the population loss. Training, however, minimizes the empirical loss against a finite training set. The exact minimizer of that empirical loss is the memorizer, the empirical training distribution, which covers zero unseen strings. Either way, the loss value alone cannot substitute for sampling the trained model.</p> <h2 id="the-benchmark">The benchmark</h2> <p>We trained thirteen generative models on the same training subsets and scored each one by free sampling.</p> <ul> <li><strong>Likelihood-trained:</strong> a tensor-network Born machine (TNBM), three transformers, three GRU recurrent networks, and a restricted Boltzmann machine.</li> <li><strong>Moment-trained:</strong> an IQP Born machine, a magic fermionic Born machine (FBM), a passive number-conserving FBM, an active FBM, and a classical generative moment-matching network.</li> </ul> <p>We used two datasets.</p> <ul> <li><strong>Cardinality-constrained:</strong> every N-bit string of Hamming weight N/2, with a uniform target, at N = 16, 20, and 30. At N = 30 we simulate full state vectors over a valid set of about 155 million strings.</li> <li><strong>Genomic single-nucleotide variants:</strong> observed variant sequences, at N = 16 and 20. Here the valid set is the observed data itself.</li> </ul> <h2 id="what-we-found">What we found</h2> <h3 id="loss-and-coverage-decouple">Loss and coverage decouple</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/tcdq/rank6_combined-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/tcdq/rank6_combined-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/tcdq/rank6_combined-1400.webp"/> <img src="/assets/img/blog/tcdq/rank6_combined.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Rank correlations among MMD², coverage, and forward KL at N = 16, for the cardinality data (top) and the genomic data (bottom). Left: the two generalization measures agree. Middle and right: the moment-matching loss correlates poorly with both. </div> <p>On the cardinality task, the moment-trained IQP and FBM models drive the MMD² to a low value and still fail to cover the valid set. The IQP Born machine and the TNBM reach the same MMD² within 40%, yet they cover 0.09 and 0.41 of the unseen valid strings. Coverage and forward KL rank the models almost the same way. MMD² ranks them differently from both.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/tcdq/fig_mechanism-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/tcdq/fig_mechanism-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/tcdq/fig_mechanism-1400.webp"/> <img src="/assets/img/blog/tcdq/fig_mechanism.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> (a) IQP at N = 16: the MMD² falls by an order of magnitude while coverage stays at the random-bits floor. (b) A likelihood-trained transformer: the NLL falls and coverage rises to the finite-sample ceiling. (c) Coverage against parameter count: the classical models and the TNBM improve with size, while IQP and the magic FBM stay flat. (d) Normalized coverage against N up to 30: the gap widens. </div> <p>We checked the easy explanations. Enlarging the IQP circuit 18× in gate count leaves its coverage flat. Sweeping the kernel bandwidth from sharp to broad keeps IQP near the random floor. Raising the query budget lifts the likelihood-trained models toward the ceiling and leaves the moment-trained ones where they were, so the low coverage is not a finite-sampling artifact.</p> <div class="row mt-3"> <div class="col-sm-9 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/tcdq/n16_grid_part1-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/tcdq/n16_grid_part1-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/tcdq/n16_grid_part1-1400.webp"/> <img src="/assets/img/blog/tcdq/n16_grid_part1.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Where the models put their probability mass. The full space of 2¹⁶ bitstrings at N = 16, rendered as a 256 × 256 grid. Gray cells lie off the Hamming-weight-8 slice. Colored cells lie on it, with intensity proportional to sample probability. The number-conserving FBM puts all its mass on the valid slice by construction. The moment-trained models assign substantial probability outside the valid sector. </div> <h3 id="structure-does-what-the-loss-cannot">Structure does what the loss cannot</h3> <p>One moment-trained model did well on the cardinality task. The passive fermionic Born machine is a matchgate circuit that only emits strings of fixed Hamming weight. Its architecture confines it to the valid set, whatever the loss does. On the genomic data, which has no fixed Hamming weight, the same model generalized as poorly as the other moment-trained models.</p> <h3 id="the-genomic-data">The genomic data</h3> <p>On the genomic variants, the transformer and the RNN reached the highest coverage, with the TNBM just behind. The moment-trained models were again the weakest generators. Loss and coverage correlated a little better than on the cardinality task, so how well MMD² tracks generalization depends on the dataset. Per-locus allele frequencies, linkage disequilibrium, and PCA projections of the samples tell the same story in the appendix.</p> <h2 id="what-this-changes">What this changes</h2> <p>TCDQ has been justified by trainability and by the hardness of sampling. Both can hold for a model that never produces a new valid sample. We think generalization should be checked directly, by sampling the trained model, before anyone reads a low training loss as success.</p> <p>Two directions look most promising to us. Replace moment matching with a loss that targets generalization, such as maximum likelihood or an objective with a coverage-aware term. Or build the constraint into the model, as the passive FBM does for fixed Hamming weight. Finding structure-preserving circuits for other domains, such as molecular validity or graph properties, is an open problem.</p>]]></content><author><name></name></author><category term="research"/><category term="quantum-machine-learning"/><category term="generative-models"/><summary type="html"><![CDATA[A converged moment-matching loss does not tell you whether a quantum generative model generalizes. We prove why, and we measure it across thirteen models at up to 30 qubits.]]></summary></entry><entry><title type="html">Scalable Message-Passing Quantum Graph Neural Networks in the Weisfeiler-Leman Hierarchy</title><link href="https://www.snehalraj.com/blog/2026/qgnn-weisfeiler-leman/" rel="alternate" type="text/html" title="Scalable Message-Passing Quantum Graph Neural Networks in the Weisfeiler-Leman Hierarchy"/><published>2026-06-25T04:30:00+00:00</published><updated>2026-06-25T04:30:00+00:00</updated><id>https://www.snehalraj.com/blog/2026/qgnn-weisfeiler-leman</id><content type="html" xml:base="https://www.snehalraj.com/blog/2026/qgnn-weisfeiler-leman/"><![CDATA[<p>Our new preprint, written with Brian Coyle, Léo Monbroussou, André J. Ferreira-Martins, Renato M. S. Farias, and Elham Kashefi, builds a quantum graph neural network that keeps the three properties of its classical counterpart. It passes messages inside the circuit, is exactly permutation equivariant, and reaches a chosen level of the Weisfeiler-Leman hierarchy. The paper is on <a href="https://arxiv.org/abs/2606.26873">arXiv</a>, and the code is on <a href="https://github.com/SnehalRaj/mp-qgnns">GitHub</a>. An <a href="/projects/qgnn-subspace/">interactive companion</a> shows the subspace the model works in as you change the register sizes.</p> <h2 id="message-passing-on-a-quantum-state">Message passing on a quantum state</h2> <p>A classical graph network refines node features over L rounds of message passing. Most quantum graph models skip this loop. They place gates along the graph’s edges and leave the aggregation to a classical readout.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/gnn_properties-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/gnn_properties-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/gnn_properties-1400.webp"/> <img src="/assets/img/blog/qgnn-weisfeiler-leman/gnn_properties.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> (a) Message passing: each node updates from its neighbours. (b) Permutation equivariance: relabelling the input relabels the output. (c) Weisfeiler-Leman expressivity: the particle number j sets the level the model reaches. </div> <p>Our model runs the loop on the quantum state, across two registers. A node register of N qubits at Hamming weight j indexes the graph. Its basis states are the j-subsets of nodes: single nodes at j = 1, edges at j = 2. An embedding register of D qubits at Hamming weight k holds the features. Its size does not grow with the graph.</p> <p>Four blocks act on these registers.</p> <ul> <li><strong>Loader</strong> V(x) writes the graph into the registers. We re-upload it at every layer, which makes the map nonlinear.</li> <li><strong>Adjacency</strong> A(G) applies a Givens rotation between each pair of node qubits. The edge weights set the angles and fix the gate order. It needs no training.</li> <li><strong>Evolution</strong> W(θ) updates the features on the embedding register only. Every node shares the same W. At (D, k) = (6, 3), 15 parameters drive a 20 × 20 rotation.</li> <li><strong>Mixer</strong> M couples the two registers once, before the readout.</li> </ul> <p>The state after \(L\) layers is</p> \[\lvert \psi(\mathbf{x}, G) \rangle = M(\boldsymbol{\theta}_M)\, \big[\, W(\boldsymbol{\theta})\, \mathcal{A}(G)\, V(\mathbf{x}) \,\big]^{L}\, \lvert \psi_0 \rangle .\] <p>The readout measures the one-particle density matrix of the embedding register, one D × D matrix per node. A small classical head maps it to node and edge features.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/architecture_standalone-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/architecture_standalone-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/architecture_standalone-1400.webp"/> <img src="/assets/img/blog/qgnn-weisfeiler-leman/architecture_standalone.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The circuit. A loader, an adjacency block, and a trainable evolution repeat for L layers. A mixer then couples the registers, and a readout returns node and edge features. </div> <p>Exact permutation equivariance follows. The edge weights fix the gate order, so a relabelling of the nodes reorders nothing. The evolution block treats every node the same way. The output is the relabelled output of the original, at every parameter value. Earlier equivariant quantum graph models tie parameters across the symmetric group. We tie none.</p> <h2 id="where-the-model-sits-in-the-weisfeiler-leman-hierarchy">Where the model sits in the Weisfeiler-Leman hierarchy</h2> <p>The Weisfeiler-Leman test measures how finely a model can tell graphs apart. The 1-WL test gives each node a colour. Each round replaces that colour with a hash of it and the multiset of its neighbours’ colours. The test declares two graphs different when their final colour histograms differ. A message-passing network can separate two graphs only if 1-WL can. A 1-WL network cannot count cycles, so it can give two distinct molecules the same prediction.</p> <p>The set-based j-WL test applies the same refinement to j-subsets of nodes. Each subset has a colour. The test refines it over the subsets that differ from it by one node. In this convention j = 2 matches 1-WL, j = 3 matches 2-WL, and j = 4 matches 3-WL.</p> <p>Our node register at Hamming weight j has one basis state per j-subset. A Givens rotation between node qubits a and b couples exactly the subsets that differ by a swap of a for b. That is the neighbourhood that defines j-WL. So one circuit iteration performs one round of j-WL refinement. We prove that for 2 ≤ j ≤ 4, at generic parameters, the model matches set-based j-WL. After L iterations it separates exactly the graphs that L rounds of j-WL separate. For j ≥ 3 this passes the 1-WL ceiling. To our knowledge it is the first quantum graph model proven to do so.</p> <p>The graph structure enters through the initial state, which fingerprints each j-subset’s induced subgraph with closed-walk counts. That fingerprint fails at j ≥ 5, so the theorem stops at j = 4.</p> <h2 id="staying-trainable-in-a-particle-number-subspace">Staying trainable in a particle-number subspace</h2> <p>Variational circuits suffer from barren plateaus: gradients vanish as the circuit grows. The effect grows with the size of the space the dynamics explore. Every gate in our model preserves Hamming weight, so the dynamics stay inside a structured subspace. The trainable block acts on the embedding register alone.</p> <p>This lets us pre-train on small graphs and deploy on larger ones. The trainable evolution acts on D qubits, and D does not depend on N. The adjacency block needs no training. Parameters fitted at one graph size initialise the next.</p> <p>The gradients hold up in simulation. We measured the per-parameter gradient variance at random initialisation for three embeddings, out to N + D = 56 qubits. At fixed (D, k) the variance is flat in N. Initialise from a model trained on 5-city instances, and the gradient stays one to two orders of magnitude above a random start. This holds at every size we test. A log-log fit favours a power law over an exponential in each case.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/trainability_2panel-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/trainability_2panel-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/trainability_2panel-1400.webp"/> <img src="/assets/img/blog/qgnn-weisfeiler-leman/trainability_2panel.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> (a) Per-parameter gradient variance against qubit count, out to 56 qubits, at (D, k) = (6, 3), (8, 4), and (10, 5). Dotted lines fall as one over the joint subspace dimension. (b) Four initialisations. Transfer from a 5-city model stays one to two orders of magnitude above a random start. </div> <p>We call this evidence heuristic. The supplement gives a polynomial lower bound under a 2-design assumption and all-pair mixer connectivity. We have not yet verified it for our nearest-neighbour mixer. The simulations are also expensive. One forward-and-backward pass takes 1 second at N = 20 and 114 seconds at N = 50 on one CPU core.</p> <p>Deployment needs a cheap readout. We can estimate the density matrix to accuracy ε in O(D³/ε²) shots, by a Hartree-Fock readout or a matchgate shadow. The D-qubit embedding register sets the cost, up to a factor polynomial in N. A computational-basis readout recovers only the diagonal and stalls at a tour ratio of 1.043 on the travelling salesman task. The matchgate readouts reach 1.006 at the same shot budget.</p> <h2 id="results">Results</h2> <p>We test the model on three datasets, with several random seeds each. The largest run, the travelling salesman problem with 50 cities, uses 56 qubits.</p> <h3 id="distinguishing-graphs-cfi">Distinguishing graphs (CFI)</h3> <p>Cai-Fürer-Immerman graphs are pairs of non-isomorphic graphs that 1-WL cannot tell apart. We use CFI(K3) on 6 vertices, separable at j = 3, and CFI(K4) on 8 vertices, separable at j = 4. We train the model as a binary classifier on 200 random relabellings of each graph, so it must learn a permutation-invariant rule.</p> <p>On both families the test accuracy jumps from chance to 100% exactly at the predicted particle number, and stays at chance below it. At a matched parameter budget, a 1-WL graph isomorphism network stays at chance on both families, while a 3-WL network (PPGN) separates both.</p> <div class="row mt-3"> <div class="col-sm-6 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/cfi-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/cfi-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/cfi-1400.webp"/> <img src="/assets/img/blog/qgnn-weisfeiler-leman/cfi.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Test accuracy on two CFI families as j rises. Accuracy reaches 100% exactly at the predicted level. The dashed line is a 1-WL GIN baseline at chance. </div> <h3 id="molecular-property-prediction-qm9">Molecular property prediction (QM9)</h3> <p>QM9 holds 130,831 small organic molecules with up to nine heavy atoms. We predict the HOMO-LUMO gap, the energy difference between a molecule’s highest occupied and lowest unoccupied orbitals. Between runs we change only j.</p> <p>The error falls as j rises: 0.398 eV at j = 1, 0.308 eV at j = 2, and 0.235 eV at j = 3. The j = 1 value is a validation estimate, since that run kept no test checkpoint; the other two are test errors. The parameter count barely moves, from 2,591 to 2,655. A classical message-passing network reaches 0.121 eV with 849,569 parameters, about 320 times more.</p> <div class="row mt-3"> <div class="col-sm-6 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/qm9-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/qm9-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/qm9-1400.webp"/> <img src="/assets/img/blog/qgnn-weisfeiler-leman/qm9.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Mean absolute error on the QM9 HOMO-LUMO gap as j rises, at near-constant parameter count. </div> <h3 id="travelling-salesman">Travelling salesman</h3> <p>The Euclidean travelling salesman problem asks for the shortest closed tour through N cities in the plane. We use the benchmark instances of Skolik et al. and run the model end to end at j = 1. An encoder maps each city’s coordinates onto the embedding register. A head scores every edge from the per-node density matrices, and a beam search of width 100 decodes a tour.</p> <p>Tour quality is the tour length over the optimal length, so 1.0 is optimal. The mean ratio is 1.008, 1.034, 1.078, 1.128, and 1.201 at N = 5, 10, 20, 30, 50. The equivariant quantum circuit of Skolik et al. reaches 1.026, 1.047, and 1.139 at N = 5, 10, 20. Our model is at or below these where the two overlap, though the pipelines optimise different objectives. Beyond N = 20 their N-qubit state vector is too large to simulate.</p> <div class="row mt-3"> <div class="col-sm-6 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/tsp-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/tsp-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/qgnn-weisfeiler-leman/tsp-1400.webp"/> <img src="/assets/img/blog/qgnn-weisfeiler-leman/tsp.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Tour ratio at j = 1 against the equivariant quantum circuit of Skolik et al., with 1.0 optimal. The 50-city run is a 56-qubit simulation. </div> <p>Two further tests use the 5-city instances. Built-in equivariance lowers the validation loss at every training-set size against a matched-parameter variant with a fixed gate order. The gains are 14%, 6%, and 16% at 500, 1,000, and 2,000 instances. At a matched parameter count of about 5,800, a classical graph convolutional network reaches a lower edge-prediction loss, 0.080 against our 0.224. At j = 1 the model sits in the same Weisfeiler-Leman class as that network.</p> <h2 id="what-this-changes">What this changes</h2> <p>This model carries the three properties of a classical graph network onto the quantum state. We prove all three. The particle number j sets the expressivity. Raise j and the model climbs the Weisfeiler-Leman hierarchy, on synthetic pairs and on a real chemistry task, at almost no cost in parameters.</p> <p>The evidence for trainability is heuristic. Barren-plateau theorems describe generic, randomly initialised circuits. Our structured, pre-trained model sits outside that class, so the theory does not settle its behaviour. We have also not run the model on hardware. A run at N = 50 needs 56 qubits and about 10⁴ two-qubit gates per forward pass, within the qubit count of current superconducting devices. The trained circuit is matchgate-compatible, so the parameters can move to hardware without retraining.</p> <p>The <a href="/projects/qgnn-subspace/">companion page</a> lets you change the register sizes and watch the active subspace move.</p>]]></content><author><name></name></author><category term="research"/><category term="quantum-machine-learning"/><category term="graph-neural-networks"/><summary type="html"><![CDATA[A quantum graph neural network that passes messages inside the circuit, is exactly permutation equivariant, and reaches a chosen level of the Weisfeiler-Leman hierarchy. We prove the three properties and test them in simulations of up to 56 qubits.]]></summary></entry><entry><title type="html">Adaptive Directional Gradients for Parameterised Quantum Circuits</title><link href="https://www.snehalraj.com/blog/2026/adaptive-directional-gradients/" rel="alternate" type="text/html" title="Adaptive Directional Gradients for Parameterised Quantum Circuits"/><published>2026-06-08T04:30:00+00:00</published><updated>2026-06-08T04:30:00+00:00</updated><id>https://www.snehalraj.com/blog/2026/adaptive-directional-gradients</id><content type="html" xml:base="https://www.snehalraj.com/blog/2026/adaptive-directional-gradients/"><![CDATA[<p>Our new preprint, with Brian Coyle, Virag Umathe, El Amine Cherrat, and Elham Kashefi, counts the circuit evaluations a gradient step needs. The parameter-shift rule needs two per parameter. We show that a much smaller number of random directional derivatives does the same job. We also build an optimiser that picks that number during training. The paper is on <a href="https://arxiv.org/abs/2606.09734">arXiv</a>.</p> <h2 id="the-measurement-cost-of-a-gradient">The measurement cost of a gradient</h2> <p>Gradient descent needs a gradient estimate at every step. A classical network gets it from backpropagation at about the cost of one forward pass.</p> <p>A quantum circuit has no such shortcut. The parameter-shift rule is the standard tool. It evaluates the loss circuit at two shifted parameter values and takes their difference. The derivative it returns is exact. A model with \(N\) parameters needs \(2N\) circuit evaluations per step, each with \(M\) shots, so \(2NM\) shots per gradient. At \(N = 1770\) that cost dominates the shot budget.</p> <p>Two known estimators drop the explicit \(N\) per step. SPSA shifts every parameter at once along one random sign vector and uses two circuit evaluations per step. Random coordinate descent (RCD) picks one parameter at random and computes its exact derivative. Neither escapes the cost. The variance of SPSA grows linearly in \(N\), and RCD needs of order \(N\) times more steps.</p> <h2 id="forward-gradients-from-random-directions">Forward gradients from random directions</h2> <p>Forward-mode automatic differentiation computes a directional derivative, \(\nabla_{v} f = v \cdot \nabla f\), for one direction \(v\). Baydin et al. showed that a few of them give a usable gradient estimate for classical networks. Draw \(V\) random directions whose components are independent with zero mean and unit variance. The quantum forward gradient estimator is</p> \[\widetilde{\nabla}^{\mathsf{F}} f(\theta) = \frac{1}{V} \sum_{\ell=1}^{V} \big(\widetilde{\nabla}^{\varepsilon}_{v^\ell} f\big)\, v^\ell, \qquad \widetilde{\nabla}^{\varepsilon}_{v} f = \frac{f^M(\theta + \varepsilon v) - f^M(\theta - \varepsilon v)}{2\varepsilon}.\] <p>Here \(f^M\) is the \(M\)-shot estimate of the circuit output. Each directional derivative is a central finite difference, so a step costs \(2V\) circuit evaluations and \(2VM\) shots. The circuits are the loss circuits with the parameters shifted along \(v\). There are no ancilla qubits and no controlled gates.</p> <p>The estimator is unbiased in the limit of small \(\varepsilon\). The loss of a quantum circuit is a trigonometric polynomial with bounded frequency, so a large step \(\varepsilon\) still gives a small bias. Shot noise enters the error as \(1/\varepsilon^2\) and the bias as \(\varepsilon^2\). On a VQE example the energy error is lowest at \(\varepsilon = 0.1\), and we use that value in every experiment.</p> <p>The number of directions \(V\) trades variance for circuit evaluations. With Rademacher directions, the second moment of the estimator is \((N + V - 1)/V\) times the squared gradient norm. At \(V = 1\) the factor is \(N\), and at \(V = N\) it is close to 2. More directions give a more precise step and cost more circuits. In the convergence bound the two effects cancel. The number of steps scales as \(N/V\), each step costs \(V\) circuits, and the total is independent of \(V\). Under shot noise at a fixed per-step budget, the noise floor is also independent of \(V\). This is a no-free-lunch result for convex losses. The savings we see come earlier in training, where a larger \(V\) finds better descent directions on a non-convex landscape.</p> <h2 id="three-known-optimisers-as-special-cases">Three known optimisers as special cases</h2> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/adaptive-directional-gradients/schematic-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/adaptive-directional-gradients/schematic-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/adaptive-directional-gradients/schematic-1400.webp"/> <img src="/assets/img/blog/adaptive-directional-gradients/schematic.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> (a) The training loop. (b) Four estimators as directions probed at M shots each. Parameter shift covers all N basis directions at cost 2NM. SPSA and RCD draw one direction at cost 2M. The forward gradient draws V random directions at cost 2VM. (c) A cartoon: about N/V times fewer shots than parameter shift for the same loss. (d) QUIVER adapts V and M over training. </div> <p>Set \(V = 1\) and draw \(v\) from the Rademacher distribution, so each component is \(+1\) or \(-1\). That gives SPSA. For RCD, set \(V = 1\) and draw \(v\) as one basis vector \(e_j\) scaled by \(N\), with the derivative from the parameter-shift rule. Enumerate all \(N\) basis vectors in one step instead of sampling them, and you have the parameter-shift rule. The \(N\)-fold variance of SPSA and the \(N\)-fold slowdown of RCD both follow from the second-moment formula.</p> <p>The new ingredient is \(V\). It runs from 1, the cheapest and noisiest estimator, to \(N\), the exact gradient at full cost. Among isotropic distributions with independent components, Rademacher directions give the smallest estimator variance at fixed \(V\).</p> <h2 id="quiver">QUIVER</h2> <p>The iCANS and gCANS optimisers keep the parameter-shift structure and give more shots to noisy gradient components. That works because each shifted circuit perturbs a different gate, so the measurement variance differs across parameters. A random direction perturbs every parameter at once, and concentration of measure makes the variance nearly the same for every direction. We prove this for local Hamiltonians on bounded-depth circuits and check it numerically. Across VQE and QAOA instances with \(N\) up to about \(10^3\), the per-direction variances sit within 15% of their mean. Per-direction shot allocation has nothing to work with.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/adaptive-directional-gradients/v_schedule-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/adaptive-directional-gradients/v_schedule-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/adaptive-directional-gradients/v_schedule-1400.webp"/> <img src="/assets/img/blog/adaptive-directional-gradients/v_schedule.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> A decaying V-schedule (blue) against its two fixed-V endpoints (grey) at a matched per-step budget. (a) QAOA MaxCut, linear, 30 to 2. (b) ECG5000 at n = 50, linear, 50 to 2. (c) MNIST at n = 50, cosine, 150 to 5. (d) VQE, exponential, 20 to 2. </div> <p>A fixed schedule for \(V\) already helps. Decay \(V\) over training while the per-step budget \(B = 2VM\) stays fixed, so \(M\) rises as \(V\) falls. On all four benchmarks a decaying schedule beat both fixed endpoints at matched shots. We present this as a heuristic, since the best shape and range vary by problem.</p> <p>QUIVER (Quantum Iterative V-adaptive Estimator Refinement) replaces the schedule with a rule computed from the measurement statistics. The optimiser keeps two exponential moving averages at no extra shot cost. One, \(\widehat{g}^2_t\), tracks the squared norm of the reconstructed gradient. The other, \(\widehat{\sigma}^2_t\), tracks the variance across the \(V\) directional derivatives. Each epoch it sets</p> \[V^\star_t = \frac{(N - 1 + \alpha)\, \widehat{g}^2_t}{\tau^2}, \qquad M^\star_t = \frac{N\, \widehat{\sigma}^2_t}{\alpha\, \widehat{g}^2_t},\] <p>with two hyperparameters: a ratio \(\alpha\) that sets the shots per direction and a target variance \(\tau^2\) for the gradient estimate. As training shrinks the gradient, \(V\) falls and \(M\) rises. Early on the optimiser probes many directions with few shots each. Later it concentrates shots on a few directions. A short warmup and a clamp on each update keep the noisy early averages from swinging \(V\) and \(M\). The derivation assumes a loss linear in circuit expectation values, which holds for VQE and QAOA.</p> <p>The rule solves a cost problem. Among all \((V, M)\) pairs that reach a target mean-squared error with at least \(M_{\min}\) shots per direction, it picks the cheapest. Its shot budget matches the Cramér-Rao lower bound for any unbiased gradient estimator on a shot-noise oracle, up to a factor \((N - 1 + \alpha)/N\). Parameter shift saturates the same bound, so the practical gain is a constant factor.</p> <h2 id="results">Results</h2> <p>We train orthogonal quantum neural networks, circuits that preserve Hamming weight, on ECG5000 time-series classification and MNIST image classification. These circuits have \(N = n(n-1)/2\) parameters on \(n\) qubits, so \(n = 60\) gives \(N = 1770\). We also run VQE on a transverse-field Ising model at \(n = 10\), with \(N = 160\). The QAOA benchmark is depth-3 MaxCut on 16-vertex weighted graphs, with \(N = 99\). Every stochastic method gets \(B = 1000\) shots per step. The parameter-shift rule gets \(M = 10\) shots per parameter, so its per-step cost grows with \(N\). All methods use Adam, and we report means over three seeds.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/adaptive-directional-gradients/four_domains-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/adaptive-directional-gradients/four_domains-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/adaptive-directional-gradients/four_domains-1400.webp"/> <img src="/assets/img/blog/adaptive-directional-gradients/four_domains.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The best forward gradient configuration against parameter shift (PS), SPSA, and RCD. (a) QAOA MaxCut, N = 99. (b) VQE at n = 10, N = 160. (c) ECG5000 at n = 60, N = 1770. (d) MNIST at n = 50, N = 1225. Bands: one standard deviation over three seeds. We omit PS from (d) for cost. </div> <h3 id="fixed-v-against-the-baselines">Fixed V against the baselines</h3> <p>On ECG5000, forward gradients with \(V = 10\) beat SPSA and RCD at every system size. They match or beat the parameter-shift rule at a fraction of the total shots. The gap to the exact gradient is 1 to 2 percentage points up to \(n = 60\). The best \(V\) is 10 at most sizes and 25 at \(n = 60\). MNIST shows the same ordering.</p> <div class="row mt-3"> <div class="col-sm-8 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/adaptive-directional-gradients/ecg_forward_vs_ps-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/adaptive-directional-gradients/ecg_forward_vs_ps-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/adaptive-directional-gradients/ecg_forward_vs_ps-1400.webp"/> <img src="/assets/img/blog/adaptive-directional-gradients/ecg_forward_vs_ps.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> ECG5000 test accuracy against total shots, for n from 10 to 60 qubits. Green: forward gradients at a fixed per-step budget of 1000 shots. Purple: parameter shift at 10 or 50 shots per parameter. Blue line: the exact gradient. </div> <p>On VQE at \(N = 160\), forward gradients with \(V = 10\) reach a best energy error of 0.115 ± 0.010 at 5 × 10⁶ shots. Adam with parameter shift reaches 0.203 ± 0.021 at the same budget, and SPSA reaches 0.215. On QAOA at \(N = 99\), forward gradients with \(V = 10\) reach an approximation ratio of 0.988, within about 0.01 of the exact gradient.</p> <h3 id="quiver-against-icans-and-gcans">QUIVER against iCANS and gCANS</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/adaptive-directional-gradients/quiver_vs_icans-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/adaptive-directional-gradients/quiver_vs_icans-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/adaptive-directional-gradients/quiver_vs_icans-1400.webp"/> <img src="/assets/img/blog/adaptive-directional-gradients/quiver_vs_icans.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> QUIVER against iCANS, gCANS, and parameter shift on VQE at n = 10 and a matched budget of 5 million shots, three seeds. (a) N = 80 at learning rate 0.05. (b) N = 160 at learning rate 0.003. </div> <p>The iCANS shot rule carries a prefactor in \(L\eta\), the Lipschitz constant times the learning rate. At \(N = 80\) and \(\eta = 0.05\) that product is about 0.95, so iCANS and gCANS adapt. QUIVER still reaches a lower final energy error. At \(N = 160\) and \(\eta = 0.003\) the product is about 0.06. The prefactor then pins iCANS and gCANS at their minimum shot count, and both collapse onto plain parameter shift. QUIVER leads by a margin well outside the seed-to-seed spread. On QAOA MaxCut at a 10⁶-shot budget, QUIVER leads iCANS at every \(N\) we tested.</p> <div class="row mt-3"> <div class="col-sm-8 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/adaptive-directional-gradients/quiver_trajectory-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/adaptive-directional-gradients/quiver_trajectory-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/adaptive-directional-gradients/quiver_trajectory-1400.webp"/> <img src="/assets/img/blog/adaptive-directional-gradients/quiver_trajectory.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> One QUIVER run on VQE at n = 8, depth 20, N = 320, three seeds. (a) V and M over epochs. (b) The signal and noise moving averages. (c, d) The energy error against epochs and against total shots. </div> <p>On that run, both \(V_t\) and \(M_t\) move after the 50-epoch warmup and span an order of magnitude. The signal average decays while the noise average stays flat, and their ratio drives the update. The energy error descends monotonically on both axes.</p> <h2 id="what-this-changes">What this changes</h2> <p>The parameter-shift rule costs \(2N\) circuits per gradient step. Forward gradients cost \(2V\) for any \(V\) from 1 to \(N\), and QUIVER picks \(V\) from statistics the training loop already produces. The circuits need no ancillas or mid-circuit measurements. The \(V\) directional derivatives are independent jobs and can run in parallel across devices.</p> <p>The convergence bound says the per-step saving cannot become an asymptotic speedup on convex losses. Yet the empirical saving grows with \(N\) on non-convex landscapes, and we do not yet know why. The noise-concentration proof covers local Hamiltonians on bounded-depth circuits, so it does not yet cover chemistry Hamiltonians or long-range spin systems.</p>]]></content><author><name></name></author><category term="research"/><category term="quantum-machine-learning"/><category term="optimization"/><summary type="html"><![CDATA[A gradient estimator for quantum circuits that averages V random directional derivatives. SPSA, random coordinate descent, and the parameter-shift rule are its limiting cases, and the QUIVER optimiser sets V from the measurement statistics.]]></summary></entry><entry><title type="html">Bayesian Quantum Orthogonal Neural Networks for Anomaly Detection</title><link href="https://www.snehalraj.com/blog/2025/bayesian-orthogonal-anomaly-detection/" rel="alternate" type="text/html" title="Bayesian Quantum Orthogonal Neural Networks for Anomaly Detection"/><published>2025-04-25T04:30:00+00:00</published><updated>2025-04-25T04:30:00+00:00</updated><id>https://www.snehalraj.com/blog/2025/bayesian-orthogonal-anomaly-detection</id><content type="html" xml:base="https://www.snehalraj.com/blog/2025/bayesian-orthogonal-anomaly-detection/"><![CDATA[<p>We wrote this paper with Natansh Mathur, Brian Coyle, and Nishant Jain at QC Ware. Our coauthors at Airbus are Akshat Tandon, Jasper Simon Krauser, and Rainer Stoessel. The paper asks how a quantum-inspired network can report its own confidence. We presented it at IEEE Quantum Week 2025, and it is on <a href="https://arxiv.org/abs/2504.18103">arXiv</a>.</p> <h2 id="the-problem">The problem</h2> <p>Additive manufacturing builds a part layer by layer, and aerospace firms have adopted it fast. The printed parts still hide internal defects, such as pores and lack of fusion.</p> <p>The standard check is a computed tomography (CT) scan, which a human expert reads. That reading is slow and error-prone. CT machines also fit only parts up to a set size.</p> <p>Supervised models can read the scans, but they need labelled data, and defects are rare. Autoencoders need no labels. An encoder compresses the 3D object to a small latent space, and a decoder rebuilds it. If the rebuild is close, the part is normal. A poor rebuild flags an anomaly, since the model never learned to reproduce outliers.</p> <p>In a safety check, the inspector needs to know how sure the model is, and an accuracy score does not tell them. Bayesian learning gives the model a way to say so.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/overview-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/overview-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/overview-1400.webp"/> <img src="/assets/img/blog/bayesian-orthogonal-anomaly-detection/overview.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> An autoencoder with orthogonal 3D convolutional layers rebuilds the object. Each layer flattens a 3D patch, loads it into a quantum state, and applies the orthogonal matrix that the circuit implements. In the Bayesian version, each circuit angle has a Gaussian with a trainable mean and variance. </div> <h2 id="orthogonal-quantum-neural-networks">Orthogonal quantum neural networks</h2> <p>A fully connected layer computes \(y = \sigma(Wx + b)\). An orthogonal layer adds the constraint \(W^\top W = I\). Orthogonal weights keep gradients from exploding or vanishing, and they come with guarantees on generalization error. Each gradient step breaks orthogonality, so training has to restore it after every update. Exact methods cost \(O(d^3)\) for a \(d \times d\) matrix. Approximate methods, such as singular value bounding, lose the guarantees.</p> <p>Landman et al. removed the need to re-orthogonalize. Write the orthogonal matrix as a quantum circuit of reconfigurable beam splitter (RBS) gates,</p> \[\mathsf{RBS}(\theta) = \begin{pmatrix} 1 &amp; 0 &amp; 0 &amp; 0 \\ 0 &amp; \cos\theta &amp; -\sin\theta &amp; 0 \\ 0 &amp; \sin\theta &amp; \cos\theta &amp; 0 \\ 0 &amp; 0 &amp; 0 &amp; 1 \end{pmatrix}.\] <p>Each RBS gate is a Givens rotation on two qubits, and it preserves Hamming weight. Load a normalized data vector into the Hamming-weight-1 subspace, \(\lvert x \rangle = \sum_j x_j \lvert e_j \rangle\). The circuit keeps the state in that subspace, so the output is \(y = Ox\) for an orthogonal matrix \(O\) fixed by the angles. The matrix is orthogonal by construction at every step of training. A \(d \times d\) layer needs \(O(d^2)\) gates in linear depth.</p> <div class="row mt-3"> <div class="col-sm-8 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/ortho_layer_8x8-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/ortho_layer_8x8-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/ortho_layer_8x8-1400.webp"/> <img src="/assets/img/blog/bayesian-orthogonal-anomaly-detection/ortho_layer_8x8.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> An 8 by 8 orthogonal layer in the pyramid layout. Each vertical bar is an RBS gate with its own angle. Figure from Landman et al. </div> <p>The unary subspace makes these circuits easy to simulate classically, so the same model runs in two modes. A classical computer simulates it at large scale today, or the circuit runs on quantum hardware. Data encoded at Hamming weight \(k &gt; 1\) lives in a subspace of dimension \(\binom{n}{k}\), which is where classical simulation gets hard. These circuits can also be free from barren plateaus, which matters once training runs on hardware at sizes a classical computer cannot simulate.</p> <h3 id="3d-orthogonal-convolutions">3D orthogonal convolutions</h3> <p>CT data lives in three dimensions, so we built a 3D convolution out of orthogonal layers. The idea, from Kerenidis, Landman, and Prakash, turns convolution into matrix multiplication. Flatten each of the \(k\) filters of size \(d \times d \times d\) into a row of a matrix \(F \in \mathbb{R}^{k \times d^3}\). The convolution is then the product of \(F\) with each flattened 3D patch of the input. We implement \(F\) with an RBS circuit, so the filters are mutually orthogonal and learn less redundant features. The layer needs \(\max(k, d^3)\) qubits, a count that does not grow with the size of the object. We call the layer OrthoConv3D.</p> <div class="row mt-3"> <div class="col-sm-8 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/conv_as_matmul-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/conv_as_matmul-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/conv_as_matmul-1400.webp"/> <img src="/assets/img/blog/bayesian-orthogonal-anomaly-detection/conv_as_matmul.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Convolution as matrix multiplication, drawn for the 2D case. The image patches form the rows of A and the flattened filters form F. We model F as an orthogonal matrix. Figure from Kerenidis, Landman, and Prakash. </div> <h2 id="making-them-bayesian">Making them Bayesian</h2> <p>Standard training finds one point estimate of the parameters. Bayesian learning treats the parameters as random variables with a posterior \(\pi(\theta \mid \mathcal{D})\) given the data \(\mathcal{D}\). A prediction averages the model’s output over that posterior, and the spread of the outputs is the model’s uncertainty.</p> <p>The direct way to make an orthogonal network Bayesian is to put a distribution on the weight matrix and orthogonalize each sample. That brings back the cubic cost or the approximation. We put the distribution on the circuit angles instead. Each angle gets an independent Gaussian, \(\theta_i \sim \mathcal{N}(\mu_i, \Sigma_i^2)\), with a standard normal prior. Every sample of the angles gives an exactly orthogonal matrix at quadratic cost.</p> <p>We learn the means and variances by variational inference. To bring the variational distribution \(q_\gamma\) close to the true posterior in KL divergence, we maximize the evidence lower bound,</p> \[\mathcal{L}(\gamma) = \mathbb{E}_{q_\gamma}\left[\log p(\mathcal{D} \mid \theta)\right] - \frac{1}{2}\sum_{i=1}^{N}\left[\Sigma_i^2 + \mu_i^2 - \log \Sigma_i^2 - 1\right].\] <p>We estimate the first term by Monte Carlo from sampled angles. At inference we sample the angles several times, run the network each time, and read off a distribution over outputs.</p> <p>We score calibration with the expected calibration error (ECE). Bin the predictions by confidence, then sum the gap between mean confidence and accuracy over the bins, weighted by bin size. A model with low ECE is right about as often as it says it is.</p> <h2 id="results">Results</h2> <h3 id="the-data">The data</h3> <p>The dataset holds about 5,000 CT scans of 3D-printed parts with defects introduced on purpose. Each scan is a \(96 \times 96 \times 96\) greyscale tensor with a mask that marks the anomalous voxels. We cut each scan into \(16 \times 16 \times 16\) blocks. A block is anomalous if any voxel in its mask is set. This turns masking into classification and multiplies the data 216-fold, so we train on a subset.</p> <h3 id="four-architectures-four-training-methods">Four architectures, four training methods</h3> <p>We tried two families of encoder and decoder. The feedforward network (FNN) has linear layers of width 128 and 64 in the encoder and 128 in the decoder. The quantum FNN (QFNN) swaps the two inner layers for orthogonal ones in the butterfly layout. The 3D-CNN uses 3D convolutions, and the 3D-QCNN uses OrthoConv3D layers. For each model we compared point-estimate gradient descent (PE), Bayesian learning, Monte Carlo dropout (MCD), and an ensemble.</p> <table> <thead> <tr> <th>Model</th> <th>ECE</th> <th>Precision</th> <th>Recall</th> <th>F1</th> </tr> </thead> <tbody> <tr> <td>FNN (PE)</td> <td>0.257</td> <td>74.09</td> <td>71.47</td> <td>72.29</td> </tr> <tr> <td>FNN (Bayesian)</td> <td>0.221</td> <td>71.68</td> <td>71.03</td> <td>71.35</td> </tr> <tr> <td>QFNN (PE)</td> <td>0.251</td> <td>73.95</td> <td>71.78</td> <td>72.84</td> </tr> <tr> <td>QFNN (Bayesian)</td> <td>0.217</td> <td>72.89</td> <td>71.65</td> <td>72.26</td> </tr> <tr> <td>3D-CNN (PE)</td> <td>0.239</td> <td>76.35</td> <td>74.97</td> <td>75.65</td> </tr> <tr> <td>3D-CNN (Bayesian)</td> <td>0.209</td> <td>73.98</td> <td>72.86</td> <td>73.41</td> </tr> <tr> <td>3D-QCNN (PE)</td> <td>0.242</td> <td>75.49</td> <td>74.12</td> <td>74.79</td> </tr> <tr> <td>3D-QCNN (Bayesian)</td> <td>0.224</td> <td>73.13</td> <td>72.37</td> <td>72.74</td> </tr> </tbody> </table> <p>Bayesian learning had the lowest ECE of the four training methods in every architecture, with MCD second and the ensemble third. The point-estimate models kept a small lead on precision, recall, and F1. So Bayesian training costs a little accuracy and improves calibration.</p> <p>Orthogonality helped the feedforward models. The QFNN beat the FNN on ECE and F1 under both training methods, with about 1.4M parameters against 1.5M. The classical 3D-CNN beat both feedforward models on every metric, which may be specific to the architectures we chose. In the 3D models, orthogonality did not help. The 3D-QCNN trailed the 3D-CNN by a little on every metric, though it has far fewer parameters.</p> <h3 id="on-ibm-brisbane">On IBM Brisbane</h3> <p>To test the quantum mode, we ran the orthogonal layers on 8 qubits of IBM’s 127-qubit Brisbane device. The first experiment feeds eight 8-dimensional vectors through one orthogonal layer with fixed angles. It measures the fidelity of the output against the ideal state. Post-selection on unary bitstrings adds error mitigation on top of IBM’s readout mitigation. We compared random input vectors with rows of a real data slice, at 1,000 and 10,000 shots.</p> <p>On random inputs, more shots did not help. On real data they did, and the 10,000-shot runs reached the highest average hardware fidelity, about 0.98. Our guess is that real data has structure that more shots can resolve, and that the same structure hides noise on gates that barely matter. We cannot promise this holds at larger sizes.</p> <div class="row mt-3"> <div class="col-sm-8 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/hw_fidelity_voxel-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/hw_fidelity_voxel-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/hw_fidelity_voxel-1400.webp"/> <img src="/assets/img/blog/bayesian-orthogonal-anomaly-detection/hw_fidelity_voxel.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Output fidelity of one orthogonal layer for eight input vectors from the anomalous voxel slice. The lines show a shot-noise simulator, a simulated noisy Brisbane, and the real device, at 1k and 10k shots. </div> <p>The second experiment puts hardware inside the full pipeline. We took an anomalous voxel, downscaled it to 16 by 16 by 16, and picked slice 14, which holds the anomaly. With a 2 by 2 by 2 kernel at stride 1, that slice needs 256 circuits on 8 qubits, each at 5,000 shots.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/voxel_slices-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/voxel_slices-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/voxel_slices-1400.webp"/> <img src="/assets/img/blog/bayesian-orthogonal-anomaly-detection/voxel_slices.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The anomalous voxel used in the hardware runs, as 16 slices of 16 by 16 pixels. Slice 14 holds the anomaly. </div> <p>We ran a growing fraction of those circuits on Brisbane and simulated the rest exactly. At the layer output, the mean squared error grew linearly with the hardware fraction and reached about 0.88 with every circuit on hardware. One level up, at the OrthoConv3D block, the worst case was about 3.5 × 10⁻³. For the whole autoencoder, it was about 10⁻⁸. The further an output is from the quantum circuits, the smaller their share of the computation, and the less their noise shows.</p> <div class="row mt-3"> <div class="col-sm-8 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/hw_mse_full_pipeline-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/hw_mse_full_pipeline-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/bayesian-orthogonal-anomaly-detection/hw_mse_full_pipeline-1400.webp"/> <img src="/assets/img/blog/bayesian-orthogonal-anomaly-detection/hw_mse_full_pipeline.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Mean squared error of the full autoencoder output against its noiseless value, as more of the 256 circuits run on Brisbane. The vertical scale is 10 to the minus 8. </div> <h2 id="what-this-changes">What this changes</h2> <p>For a manufacturer, the result is a defect detector that reports its own confidence. Its layers stay exactly orthogonal with no cubic orthogonalization step, because the distribution is on the circuit angles.</p> <p>The hardware runs show what the quantum version looks like on a current device. One 8-qubit layer loses a few percent of fidelity, and almost none of that reaches the output of the pipeline. As devices grow, the same layers can take inputs at higher Hamming weight, where classical simulation gets hard.</p> <p>We used a mean-field Gaussian for the variational family and left richer families for later. We also compared Bayesian learning only against dropout and ensembles. Other Bayesian methods for quantum circuits, such as Langevin sampling, remain untested on orthogonal layers.</p>]]></content><author><name></name></author><category term="research"/><category term="quantum-machine-learning"/><category term="anomaly-detection"/><summary type="html"><![CDATA[We put Gaussian distributions on the angles of orthogonal quantum layers, build a 3D convolutional autoencoder from them, and use it to find defects in 3D-printed parts. Part of the pipeline runs on IBM Brisbane.]]></summary></entry><entry><title type="html">QuIC: Quantum-Inspired Compound Adapters for Parameter Efficient Fine-Tuning</title><link href="https://www.snehalraj.com/blog/2025/quic-compound-adapters/" rel="alternate" type="text/html" title="QuIC: Quantum-Inspired Compound Adapters for Parameter Efficient Fine-Tuning"/><published>2025-02-10T04:30:00+00:00</published><updated>2025-02-10T04:30:00+00:00</updated><id>https://www.snehalraj.com/blog/2025/quic-compound-adapters</id><content type="html" xml:base="https://www.snehalraj.com/blog/2025/quic-compound-adapters/"><![CDATA[<p>Our preprint, written with Brian Coyle at QC Ware, asks how few parameters a fine-tuning adapter can have and still do useful work. We build the adapter from one small orthogonal matrix and its compound matrices, a structure taken from Hamming-weight preserving quantum circuits. It fine-tunes DeBERTa, DINOv2, and LLaMA with less than 0.02% of the base model’s memory. The paper is on <a href="https://arxiv.org/abs/2502.06916">arXiv</a>.</p> <h2 id="fine-tuning-with-almost-no-new-parameters">Fine-tuning with almost no new parameters</h2> <p>A foundation model holds weight matrices \(W^* \in \mathbb{R}^{d \times d}\) in its attention and feed-forward layers. Full fine-tuning updates all of them. For a model with billions of parameters, that costs too much memory and time. Parameter-efficient fine-tuning (PEFT) freezes \(W^*\) and trains a small adapter \(\Delta W\) instead. Additive methods set \(W_{\mathrm{adapt}} = W^* + \Delta W\). Multiplicative methods set \(W_{\mathrm{adapt}} = \Delta W \, W^*\).</p> <p>LoRA is the best-known additive method. It writes \(\Delta W = \alpha W_{\mathrm{up}} W_{\mathrm{down}}\) with two thin matrices of rank \(r \ll d\). On DeBERTaV3-base, LoRA at rank 8 still trains 1.33 million parameters. Some settings need far less. Think of a personal model that lives on a phone, or a server that holds thousands of task-specific adapters at once.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quic-compound-adapters/overview_adapters-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quic-compound-adapters/overview_adapters-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quic-compound-adapters/overview_adapters-1400.webp"/> <img src="/assets/img/blog/quic-compound-adapters/overview_adapters.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Four ways to fine-tune one weight matrix, with trainable parameters in dark green. (a) Full fine-tuning. (b) LoRA adds a product of two thin matrices. (c) OFT multiplies W* by a block-diagonal orthogonal matrix. (d) QuIC trains only the small base matrix at the top left of each block and computes the compound blocks from it. </div> <h2 id="orthogonal-adapters">Orthogonal adapters</h2> <p>Orthogonal fine-tuning (OFT) is a multiplicative method. Its adapter satisfies \(\Delta W^\top \Delta W = \mathbb{1}\). An orthogonal map keeps the norms of feature vectors and the angles between them. The fine-tuned layer keeps the spectral properties of \(W^*\). The OFT authors argue that this protects the pretrained knowledge.</p> <p>OFT keeps \(\Delta W\) orthogonal with the Cayley transform. For a skew-symmetric matrix \(Q\), the matrix \((\mathbb{1} + Q)(\mathbb{1} - Q)^{-1}\) is orthogonal with determinant one. We update \(Q\) during training, and the adapter stays orthogonal at every step. To cut parameters, OFT makes \(\Delta W\) block-diagonal with \(r\) blocks of size \(d/r\). That costs \(O(d^2/r)\) parameters, or \(O(d^2/r^2)\) if the blocks share weights. BOFT writes the adapter as a product of sparse butterfly factors and reaches \(O(d \log d)\).</p> <p>Our adapter starts from OFT and compresses each block further.</p> <h2 id="compound-adapters">Compound adapters</h2> <p>Take a base matrix \(A \in \mathbb{R}^{n \times n}\). Its compound matrix of order \(k\) is the \(\binom{n}{k} \times \binom{n}{k}\) matrix whose entries are the \(k \times k\) minors of \(A\):</p> \[\mathcal{C}_k := A^{(k)}, \qquad A^{(k)}_{IJ} := \det(A_{IJ}), \qquad I, J \subseteq [n], \quad \lvert I \rvert = \lvert J \rvert = k.\] <p>Here \(A_{IJ}\) is the submatrix of \(A\) with rows \(I\) and columns \(J\). The first compound is \(A\) itself. The second has \(\binom{n}{2}\) rows, so a \(44 \times 44\) base matrix defines a \(946 \times 946\) compound.</p> <p>A circuit of fermionic beam splitter gates preserves Hamming weight. On the weight-one part of its input it applies some orthogonal matrix \(A\). Kerenidis et al. showed that on the weight-\(k\) part it applies the compound matrix \(A^{(k)}\).</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quic-compound-adapters/hw_compound_circuit-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quic-compound-adapters/hw_compound_circuit-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quic-compound-adapters/hw_compound_circuit-1400.webp"/> <img src="/assets/img/blog/quic-compound-adapters/hw_compound_circuit.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> A Hamming-weight preserving circuit and its matrix. Left: loaders write data into the weight-1, weight-2, and weight-3 subspaces, then one trainable layer acts on all of them. Right: that layer is a block-diagonal unitary U. Each block is a compound matrix of one base matrix, and it acts on the states of one Hamming weight. </div> <p>We call the adapters quantum-inspired because, for a constant order \(k\), a classical computer can apply \(A^{(k)}\) directly to each subspace.</p> <p>A QuIC adapter copies this block structure. Split \(\Delta W_Q\) into \(N\) diagonal blocks of size \(b = d/N\). Inside block \(i\), place the direct sum of the compounds of one base matrix \(A_i\) up to a maximum order \(K\). Then pad with an identity to fill the block:</p> \[\Delta W_Q = \bigoplus_{i=1}^{N} \Delta W_Q^i, \qquad \Delta W_Q^i := \begin{bmatrix} \bigoplus_{k=1}^{K} A_i^{(k)} &amp; 0 \\ 0 &amp; \mathbb{1}_{b - d_{\mathrm{comp}}} \end{bmatrix}, \qquad d_{\mathrm{comp}} := \sum_{k=1}^{K} \binom{n}{k}.\] <p>The base size \(n\) is the largest integer with \(d_{\mathrm{comp}} \le b\). For \(d = 1024\), one block, and \(K = 2\), that gives \(n = 44\): a \(44 \times 44\) block for \(A\), a \(946 \times 946\) block for \(A^{(2)}\), and 34 padded dimensions. With \(K = 1\) the adapter is OFT.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quic-compound-adapters/adapter_configs-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quic-compound-adapters/adapter_configs-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quic-compound-adapters/adapter_configs-1400.webp"/> <img src="/assets/img/blog/quic-compound-adapters/adapter_configs.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Three QuIC configurations. Left: C1 only with four blocks, which is OFT when A is orthogonal. Middle: C1 plus C2 with four blocks. Right: C1 plus C2 plus C3 with two blocks. The trailing dimensions of each block hold an identity matrix and are not trained. </div> <p>If \(A\) is orthogonal, every compound \(A^{(k)}\) is orthogonal. That follows from the Cauchy-Binet formula. The identity padding is orthogonal too, so the whole adapter is. The Cayley parametrization keeps it so during training.</p> <p>Every trainable parameter lives in the base matrices. With orthogonality, block \(i\) trains \(n(n-1)/2\) numbers, so the adapter trains \(N n(n-1)/2\) in total. A larger \(K\) forces a smaller \(n\), so higher orders mean fewer parameters.</p> <p>The forward pass costs \(O(d^2/N)\). Building the compounds from \(A\) is a one-time cost, polynomial in \(n\) for fixed \(K\).</p> <h2 id="results">Results</h2> <p>We fine-tune three backbones on four kinds of task: DeBERTaV3-base on language, DINOv2-large on vision, and LLaMA-7B on math and reading comprehension. Unless we say otherwise, the QuIC configuration is \(\mathcal{C}_1 \oplus \mathcal{C}_2\) with three blocks, orthogonality on, and no weight sharing across blocks.</p> <p>We combine accuracy and parameter count in one Pareto score. It is the accuracy gain over the zero-shot baseline, divided by \(\log_{10}\) of the parameter count in thousands.</p> <h3 id="language">Language</h3> <p>On five GLUE tasks, the QuIC adapter trains 0.03 million parameters and stores them in 0.12 MB. LoRA at rank 8 needs 1.33 million and 5.3 MB, so QuIC is over 40 times smaller. Full fine-tuning trains 184 million, so the adapter is under 0.02% of the base model. QuIC’s mean score is 85.57, against 86.30 for LoRA and 88.50 for BOFT. Its Pareto score of 28.14 is the highest in the table, and it sits on the Pareto frontier with LoKr, AdaLoRA, and BOFT.</p> <div class="row mt-3"> <div class="col-sm-8 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quic-compound-adapters/pareto_glue-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quic-compound-adapters/pareto_glue-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quic-compound-adapters/pareto_glue-1400.webp"/> <img src="/assets/img/blog/quic-compound-adapters/pareto_glue.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Mean GLUE score against adapter memory on a log scale, for DeBERTaV3-base. Blue points lie on the Pareto frontier. QuIC (star) is the smallest adapter on the frontier at 0.12 MB. </div> <h3 id="vision">Vision</h3> <p>On five VTAB-1k tasks, QuIC trains 0.13 million parameters against 1.77 million for LoRA at rank 4. Each task gives 1,000 labeled images for training and validation, and we report accuracy on the full test set. Mean accuracy is 82.61; LoRA gets 83.24 and BOFT 83.82. On CIFAR-100 QuIC scores 87.5, about ten points above every other method in the table.</p> <h3 id="math-and-reasoning">Math and reasoning</h3> <p>On LLaMA-7B we use four blocks per adapter, which gives 0.5 million parameters. LoRA at rank 32 has 58.1 million on the math tasks and QuanTA has 13.3 million. On four MATH10K word-problem sets, QuIC averages 52.1, against 65.6 for LoRA and 64.5 for QuanTA. It is best on AQuA at 24.8 and well behind on GSM8K. DROP is closer: 52.6 for QuIC, 54.0 for LoRA, 59.5 for QuanTA. The gap to LoRA is under one point on GLUE and about thirteen points on the math sets. That is the trade at this level of compression.</p> <h3 id="what-the-ablations-say">What the ablations say</h3> <p>We ran ablations on STS-B, one of the GLUE tasks.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quic-compound-adapters/config_params_accuracy-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quic-compound-adapters/config_params_accuracy-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quic-compound-adapters/config_params_accuracy-1400.webp"/> <img src="/assets/img/blog/quic-compound-adapters/config_params_accuracy.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> STS-B accuracy (line, right axis) and trainable parameters (bars, left axis, log scale) for seven compound configurations. Every configuration that includes C1 scores above 88. Every configuration without it scores near 40. </div> <p>Without the first-order compound, nothing else helps. \(\mathcal{C}_1\) alone, which is OFT, scores 91.68 with 1.8 million parameters. Add \(\mathcal{C}_2\) and the count drops to 33 thousand, for a score of 88.85. Add \(\mathcal{C}_3\) too and it drops to 13 thousand, for 88.48. Remove \(\mathcal{C}_1\) and the score falls to about 40, whatever else is present. We think the gradient has trouble reaching \(A\) through the determinant when \(A\) itself is absent.</p> <p>Orthogonality matters as much. Averaged over the seven configurations, orthogonal adapters score 68.70 and non-orthogonal ones 27.32. For \(\mathcal{C}_1 \oplus \mathcal{C}_2\) the constraint lifts the score from 33.29 to 88.85.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quic-compound-adapters/orthogonality_stsb-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quic-compound-adapters/orthogonality_stsb-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quic-compound-adapters/orthogonality_stsb-1400.webp"/> <img src="/assets/img/blog/quic-compound-adapters/orthogonality_stsb.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> STS-B accuracy with and without the orthogonality constraint, for each compound configuration. The constraint helps in every case. It is decisive whenever a higher-order compound is present. </div> <p>We also replaced the determinant on the minors with the elementwise maximum and with the average. Both do far worse. Our reading is that max and avg break orthogonality, which a multiplicative adapter needs.</p> <p>Once a higher-order compound is in the adapter, the parameter count drops sharply, and we cannot tune it finely. To add capacity back, we multiply several adapters together. Four \(\mathcal{C}_1 \oplus \mathcal{C}_2\) adapters in a product have 0.14 million parameters. They lift STS-B from 90.16 to 91.44 and CoLA from 64.57 to 65.83, while RTE and MRPC fall a little.</p> <h2 id="what-this-changes">What this changes</h2> <p>QuIC runs at parameter budgets below any other method in our tables. BOFT gives the best mean accuracy on GLUE and VTAB with 0.75 and 1.99 million parameters. At 0.03 and 0.13 million, QuIC keeps most of that accuracy. OFT is QuIC at \(K = 1\), so one framework covers both ends.</p> <p>The construction also gives a direct route to quantum hardware. Fixed Hamming-weight loaders write the input into the weight-1 to weight-\(K\) subspaces, and a layer of beam splitter gates applies the compounds. Train the gate angles instead of the Cayley parameters, and the trained adapter is already a compiled circuit. Other quantum-inspired adapters lack this. QuanTA’s tensors have no efficient general compilation to unitaries. QPA reads \(2^N\) outcome probabilities from a circuit. For a billion adapter parameters, that needs about 10,000 billion shots at 0.01 accuracy.</p> <p>We left two things for later. One is additive QuIC adapters in the style of LoRA. The other is larger \(K\), where the compound circuits become hard to simulate classically and a quantum implementation could pay off.</p>]]></content><author><name></name></author><category term="research"/><category term="parameter-efficient-fine-tuning"/><category term="quantum-inspired"/><summary type="html"><![CDATA[An adapter built from one small orthogonal matrix and its compound matrices fine-tunes DeBERTa, DINOv2, and LLaMA with under 0.02% of the base model's memory, over 40 times smaller than LoRA.]]></summary></entry><entry><title type="html">Training-Efficient Density Quantum Machine Learning</title><link href="https://www.snehalraj.com/blog/2024/density-quantum-machine-learning/" rel="alternate" type="text/html" title="Training-Efficient Density Quantum Machine Learning"/><published>2024-05-30T04:30:00+00:00</published><updated>2024-05-30T04:30:00+00:00</updated><id>https://www.snehalraj.com/blog/2024/density-quantum-machine-learning</id><content type="html" xml:base="https://www.snehalraj.com/blog/2024/density-quantum-machine-learning/"><![CDATA[<p>Our paper on density quantum neural networks asks how to make a quantum model bigger without making its gradients more expensive. My coauthors are Brian Coyle, Natansh Mathur, El Amine Cherrat, Nishant Jain, Sofiene Kazdaghli, and Iordanis Kerenidis at QC Ware. The paper appeared on <a href="https://arxiv.org/abs/2405.20237">arXiv</a> in May 2024 and in <a href="https://www.nature.com/articles/s41534-025-01099-6">npj Quantum Information</a> in 2025. There is a recorded talk from <a href="https://www.youtube.com/watch?v=1Dp_U6U6hGQ">QTML 2024</a>.</p> <h2 id="why-training-a-quantum-model-is-expensive">Why training a quantum model is expensive</h2> <p>A quantum neural network is usually one parametrized circuit. To train it you need the gradient of a loss with respect to every parameter. The standard tool is the parameter-shift rule. It needs separate circuits for every parameter, so a circuit with \(N\) parameters costs \(O(N)\) circuits per gradient step.</p> <p>Abbas et al. estimated the limit. With one day of compute and plausible clock speeds, the parameter-shift rule reaches about 9,000 parameters on a 100-qubit circuit. Classical deep learning runs billions of parameters because backpropagation costs about as much as one forward pass, up to a log factor. Most quantum circuits have no such scaling.</p> <p>There are exceptions. Bowles et al. showed that commuting-block circuits admit a backpropagation scaling. When the generators in a block commute, and each one commutes or anticommutes with the measured observable, the gradient observables commute too. One circuit, with a diagonalizing unitary appended, reads all of them at once. The price is expressivity. A single block of commuting gates is a restricted circuit, and some such circuits are easy to simulate classically.</p> <p>A useful circuit must be hard to simulate classically and still cheap to train. Density models are one way to get a bigger model without giving up cheap gradients.</p> <h2 id="density-models">Density models</h2> <p>A density quantum neural network prepares a mixture of trainable unitaries. Take \(K\) circuits \(U_1(\theta_1), \dots, U_K(\theta_K)\) and a probability vector \(\alpha\). Load the data into a state \(\rho(x)\). The model is</p> \[\rho(\theta, \alpha, x) := \sum_{k=1}^K \alpha_k \, U_k(\theta_k) \, \rho(x) \, U_k^\dagger(\theta_k), \qquad \sum_{k=1}^K \alpha_k = 1 .\] <p>The output is the expectation of an observable, \(f = \mathrm{Tr}(\mathcal{O}\rho)\). The circuit parameters \(\theta_k\) and the weights \(\alpha_k\) are both trainable, and the weights may also depend on the input, \(\alpha_k(x)\).</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig1_density_schematic-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig1_density_schematic-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig1_density_schematic-1400.webp"/> <img src="/assets/img/blog/density-quantum-machine-learning/fig1_density_schematic.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Three ways to combine K sub-unitaries. (a) A linear combination of unitaries prepares a pure state and needs an ancilla register plus post-selection. (b) A deterministic circuit prepares the density state exactly, with controlled sub-unitaries. (c) The randomised version runs one sub-unitary per shot, chosen with probability α<sub>k</sub>. It needs no ancillas and no controlled gates. </div> <p>There are two ways to prepare this state on hardware. A deterministic circuit prepares it exactly, with an ancilla register and a controlled version of each sub-unitary. The controlled gates are expensive. The second way samples from \(\alpha\). On each shot, draw an index \(k\) with probability \(\alpha_k\) and run only \(U_k\). The average over many shots is the mixture. Its cost per forward pass is the cost of the most expensive sub-unitary.</p> <p>A linear combination of unitaries (LCU) model prepares the pure state \(\sum_k \alpha_k U_k \lvert x \rangle\) instead. That needs post-selection on an ancilla register, so a forward pass succeeds only with some probability.</p> <h2 id="what-the-mixture-buys-you">What the mixture buys you</h2> <h3 id="gradients-split-across-sub-unitaries">Gradients split across sub-unitaries</h3> <p>The model is linear in the sub-unitaries, so its gradient is a weighted sum of their gradients. If the sub-unitaries share no parameters, an unbiased gradient estimate costs \(\sum_k T_k\) circuits, where \(T_k\) is what sub-unitary \(k\) costs on its own. Take commuting-block sub-unitaries with \(B_k\) blocks each. Then the full gradient costs \(O(2\sum_k B_k - K)\) circuits on \(n+1\) qubits. With \(K = O(\log N)\), the density model keeps the backpropagation scaling of its parts.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig3_hwe_two_paths-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig3_hwe_two_paths-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig3_hwe_two_paths-1400.webp"/> <img src="/assets/img/blog/density-quantum-machine-learning/fig3_hwe_two_paths.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> A hardware-efficient circuit with D layers on n qubits, and two density versions. Left: each layer becomes one sub-unitary, and the gradient needs 2D circuits where the pure circuit needs 2nD. Right: K copies of the full circuit become the sub-unitaries, with K times more parameters and 2nDK gradient circuits. </div> <p>The first route cuts the circuit into its layers and makes each layer a sub-unitary. Each piece is a commuting-generator circuit, so a constant number of circuits gives all of its gradients. For a hardware-efficient ansatz with \(D\) layers on \(n\) qubits, the parameter-shift rule needs \(2nD\) circuits. The density version needs \(2D\). A pyramid orthogonal circuit goes from \(O(n^2)\) gradient circuits to \(O(1)\), and a round-robin circuit from \(O(n^2)\) to \(O(n)\).</p> <p>The second route keeps the circuit whole and takes \(K\) copies with independent parameters. Gradients cost \(K\) times more, but the copies run in parallel, and the model has \(K\) times more parameters.</p> <h3 id="the-mixing-lemma">The Mixing lemma</h3> <p>Why should a random mixture do as well as a linear combination? The Hastings-Campbell Mixing lemma comes from randomised compiling. If a linear combination of unitaries approximates a target well, the random mixture of the same unitaries approximates it almost as well. We prove a supervised-learning version. Suppose each of \(K\) trained sub-unitaries predicts a target function with error at most \(\delta_1\). Suppose some LCU of them predicts it with error at most \(\delta_2\). Then the density model with the same weights has error at most</p> \[\frac{\delta_1^2}{4 \lVert \mathcal{O} \rVert_\infty} + 2\delta_2 .\] <p>If the LCU cuts the error quadratically, \(\delta_2 = \delta_1^2\), the density model is also an \(O(\delta_1^2)\) predictor at the cost of one sub-unitary. The lemma assumes the target is itself a quantum model with a known observable. In the experiments we skip the LCU step and train the density model directly.</p> <h3 id="dropout-and-mixtures-of-experts">Dropout and mixtures of experts</h3> <p>Dropping \(K-1\) sub-unitaries on each shot looks like dropout. We argue the analogy is incomplete. Classical dropout combines an exponential number of sub-networks at inference, while a density model with a backpropagation scaling has at most \(O(\log N)\) sub-unitaries. The closer classical relative is the mixture of experts. A gating network sets the weights \(\alpha_k(x)\) for each input, so the sub-unitaries act as experts.</p> <h2 id="results">Results</h2> <p>We tested three model families in simulation.</p> <h3 id="equivariant-circuits-on-bars-and-dots">Equivariant circuits on bars and dots</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig5_equivariant_results-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig5_equivariant_results-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig5_equivariant_results-1400.webp"/> <img src="/assets/img/blog/density-quantum-machine-learning/fig5_equivariant_results.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Test accuracy on the noisy bars-and-dots task at 10 qubits, by (a) epochs and (b) measurement shots. Dashed lines are the base models from Bowles et al. Solid lines are density versions with two sub-unitaries, started from the pretrained base model at epoch 1000. Mean and standard deviation over 5 runs. </div> <p>We ran the translation-invariant bars-and-dots task from Bowles et al. at 10 qubits with noise \(\sigma = 1.8\). The base model is their commuting-generator XX circuit. The density version adds a second sub-unitary with the same structure and Pauli-Y generators, so both parts keep cheap gradients. We set the weights to \(\alpha = (0.99, 0.01)\) at the start, biased toward the pretrained XX model, and trained for 1,000 more epochs. Test accuracy rose by 5.8%. Density versions of a non-commuting equivariant circuit and a QCNN, each built from two independently trained copies, gained 2.6% and 3.4%.</p> <h3 id="orthogonal-circuits-on-mnist">Orthogonal circuits on MNIST</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig7_round_robin_results-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig7_round_robin_results-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig7_round_robin_results-1400.webp"/> <img src="/assets/img/blog/density-quantum-machine-learning/fig7_round_robin_results.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Round-robin orthogonal circuits on MNIST at 10 qubits. Top: the full circuit (red) against a density model built from its 9 layers (blue), by epochs (a) and by shots (b). Thin lines are sub-unitaries pretrained alone for 25 epochs. Bottom: sub-unitary depth D = 1, 2, 3, 4, and n minus 1, by epochs (c) and by shots on a log scale (d). </div> <p>Hamming-weight-preserving circuits built from RBS gates act on a unary-encoded vector as an orthogonal matrix. They are interpretable and stable to train, but the parameter-shift rule needs \(O(n^2)\) circuits for a round-robin layout. We cut the round-robin circuit into its \(n-1\) layers. Each layer holds \(n/2\) commuting gates, so each sub-unitary’s gradient costs one circuit. At \(n = 10\), a gradient step costs 9 circuits. The full circuit needs 180. We reduced MNIST to 10 features by PCA. The shallow density model reached almost the same test accuracy as the full circuit with a small fraction of the shots.</p> <p>A single orthogonal circuit, however deep, outputs one orthogonal matrix. A density model outputs a convex combination of orthogonal matrices, which is a larger class. Deeper sub-unitaries raised test accuracy at every step, from about 0.57 at \(D = 1\) to about 0.86 at \(D = n-1\). Gradient cost rose from \(O(n)\) to \(O(n^3)\) circuits. In all of these runs a linear gating network predicted the weights from the input.</p> <h3 id="data-reuploading-and-overfitting">Data reuploading and overfitting</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig8_reuploading_overfitting-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig8_reuploading_overfitting-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/density-quantum-machine-learning/fig8_reuploading_overfitting-1400.webp"/> <img src="/assets/img/blog/density-quantum-machine-learning/fig8_reuploading_overfitting.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> (a) A single-qubit data reuploading circuit with L reuploads, and a density version with 5 sub-unitaries of D reuploads each. The curves show the partial Fourier series each sub-unitary learned. (b) Test mean squared error and the train/test gap on a Chebyshev polynomial regression task, against L, at matched parameter counts. </div> <p>A single-qubit data reuploading circuit computes a partial Fourier series in its input. More reuploads mean more frequencies. We compared a circuit with \(L\) reuploads and \(3L\) parameters to a density model with five sub-unitaries of depth \(D\), with \(5 \times 3D \approx 3L\). The task is to regress a Chebyshev polynomial from noisy training data. Past about 40 reuploads the single circuit overfits. Its test error and its train/test gap both climb. The density model’s test error stays low at every \(L\) where the single circuit overfits. Each sub-unitary is a shorter Fourier series, so the mixture overfits less than one deep circuit does.</p> <h2 id="what-this-changes">What this changes</h2> <p>A density model gives a practitioner one more design choice. Cut a circuit into commuting pieces and it trains at backpropagation cost. Or stack copies. The model grows \(K\)-fold and its gradients run in parallel. The Mixing lemma says the randomised version keeps the accuracy of the far more expensive linear combination. On the tasks we tried, the density model matched or beat its pure-state parent. It cost less to train, or it overfit less at equal size.</p> <p>Barren plateaus are a property of the sub-unitaries, and mixing does not remove them. If the sub-unitaries have different gradient behaviour, the easiest one to train can dominate. The mixture-of-experts literature calls this expert collapse. Whether a density version of a circuit stays hard to simulate classically is an open question. The next step is to find more circuit families with cheap gradients and lift them into density versions. We did that here for equivariant and orthogonal circuits.</p>]]></content><author><name></name></author><category term="research"/><category term="quantum-machine-learning"/><category term="trainability"/><summary type="html"><![CDATA[A density quantum neural network is a mixture of trainable circuits. Cut a circuit into commuting pieces and the mixture trains at backpropagation cost. Stack copies and it grows without a deeper circuit.]]></summary></entry><entry><title type="html">Quantum Deep Hedging</title><link href="https://www.snehalraj.com/blog/2023/quantum-deep-hedging/" rel="alternate" type="text/html" title="Quantum Deep Hedging"/><published>2023-03-29T04:30:00+00:00</published><updated>2023-03-29T04:30:00+00:00</updated><id>https://www.snehalraj.com/blog/2023/quantum-deep-hedging</id><content type="html" xml:base="https://www.snehalraj.com/blog/2023/quantum-deep-hedging/"><![CDATA[<p>El Amine Cherrat, Iordanis Kerenidis, and I wrote this paper at QC Ware with colleagues at JPMorgan Chase. It brings quantum neural networks into Deep Hedging, the reinforcement learning approach to hedging a derivatives book. We build two kinds of quantum layer and prove that they train without barren plateaus. Then we run the trained models on a trapped-ion processor. The paper appeared on <a href="https://arxiv.org/abs/2303.16585">arXiv</a> in March 2023 and in <a href="https://quantum-journal.org/papers/q-2023-11-29-1191/">Quantum</a> in November 2023.</p> <h2 id="hedging-as-a-learning-problem">Hedging as a learning problem</h2> <p>Hedging means trading the underlying asset to reduce the risk of a derivative. Classical finance gives the optimal hedge in a frictionless market. Real markets have transaction costs and limited liquidity, so traders must adapt the textbook hedge.</p> <p>Deep Hedging, introduced by Buehler et al., turns this into a finite-horizon Markov decision process whose horizon \(T\) is the maturity. The state \(s_t\) holds the market history. A deterministic policy maps it to a trade, \(a_t = \pi_t(s_t)\). Trades have no market impact, so the next state does not depend on the trade. The reward is the cashflow minus the transaction cost.</p> <p>The trading goal is risk-adjusted. We use the exponential utility with risk aversion \(\lambda &gt; 0\),</p> \[\mathcal{E}_\lambda[X] = -\frac{1}{\lambda} \log \mathbb{E}[\exp(-\lambda X)],\] <p>applied to the cumulative return. With this goal the value function no longer satisfies the standard Bellman equation. Policy-search Deep Hedging trains one policy network by gradient descent to maximize the sampled utility. Actor-critic Deep Hedging trains a value network as well and uses it to update the policy.</p> <h2 id="quantum-models-for-the-policy-and-the-value">Quantum models for the policy and the value</h2> <p>Both kinds of layer are circuits of RBS gates. An RBS gate rotates the pair \(\lvert 01 \rangle, \lvert 10 \rangle\) by an angle \(\theta\) and leaves \(\lvert 00 \rangle\) and \(\lvert 11 \rangle\) alone. A circuit of them never changes the number of ones in a basis state.</p> <h3 id="orthogonal-layers">Orthogonal layers</h3> <p>An orthogonal layer on \(n\) qubits works in the unary basis, the \(n\) basis states with one bit set. A log-depth loader maps a vector \(\boldsymbol{x} \in \mathbb{R}^n\) to a unary state with amplitudes \(x_i / \lVert \boldsymbol{x} \rVert\). The RBS circuit then acts on that state as an \(n \times n\) orthogonal matrix. Tomography reads out the \(n\)-dimensional output at low cost. We apply a classical nonlinearity and reload for the next layer.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quantum-deep-hedging/orthogonal_layers-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quantum-deep-hedging/orthogonal_layers-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quantum-deep-hedging/orthogonal_layers-1400.webp"/> <img src="/assets/img/blog/quantum-deep-hedging/orthogonal_layers.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Three circuits for an orthogonal layer. Each vertical line is an RBS gate with its own angle. Pyramid (left) and Brick (middle) use n(n-1)/2 gates on nearest-neighbour qubits. Butterfly (right) has logarithmic depth and (n/2) log n gates but needs all-to-all connectivity. </div> <p>The circuit can only produce orthogonal matrices, so the layer needs no re-orthogonalization step. A classical computer can simulate each layer in \(O(n^2)\) time. We therefore train by simulation and keep the quantum processor for inference. We drop these layers into feed-forward, recurrent, and attention blocks in place of the linear layers.</p> <h3 id="compound-layers">Compound layers</h3> <p>The same circuit also acts on every other Hamming-weight subspace. On the subspace of weight \(k\) it acts as the \(k\)-th compound matrix \(A^{(k)}\) of the orthogonal matrix \(A\), whose entries are the \(k \times k\) minors of \(A\). The unitary is block diagonal, with one block of size \(\binom{n}{k}\) per weight.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quantum-deep-hedging/compound_block_diagonal-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quantum-deep-hedging/compound_block_diagonal-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quantum-deep-hedging/compound_block_diagonal-1400.webp"/> <img src="/assets/img/blog/quantum-deep-hedging/compound_block_diagonal.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> A compound layer U(theta) is block diagonal. Block k acts on the basis states of Hamming weight k and equals the k-th compound matrix of one n by n orthogonal matrix. </div> <p>A compound layer explores an exponentially large space with \(O(n^2)\) angles. The data loader decides how much of that space the layer uses. Load into the unary basis and the layer is orthogonal. Load with a Hadamard on each qubit and every weight takes part.</p> <div class="row mt-3"> <div class="col-sm-8 mx-auto mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quantum-deep-hedging/compound_nn-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quantum-deep-hedging/compound_nn-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quantum-deep-hedging/compound_nn-1400.webp"/> <img src="/assets/img/blog/quantum-deep-hedging/compound_nn.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> A quantum compound neural network. A data loader prepares the input state, a compound layer acts on it, and a computational-basis measurement on every qubit ends the circuit. </div> <h3 id="trainability">Trainability</h3> <p>Highly expressive circuits hit barren plateaus, where the gradient variance shrinks exponentially in the qubit count. We prove that compound layers avoid this. Take Gaussian initialization with variance \(O(1/n^2)\) and a single-qubit \(Z\) observable. For a uniform superposition as input, over all basis states or over those of weight \(k\), the expected squared gradient norm is \(\Omega(1/\mathrm{poly}(n))\). The proof computes the gradient at \(\boldsymbol{\theta} = 0\) by hand and applies a result of Zhang et al.</p> <h2 id="training">Training</h2> <h3 id="orthogonal-policies-in-a-classical-market">Orthogonal policies in a classical market</h3> <p>A quantum policy network reads the price history and outputs the hedge position at each step. We train it with policy-search Deep Hedging. Sample \(N\) paths and run the policy along each one. Then update the parameters by gradient descent on the negative sampled utility of the total returns.</p> <h3 id="a-quantum-market-and-a-distributional-critic">A quantum market and a distributional critic</h3> <p>The second method is quantum-native. Each market observation becomes an \(n\)-bit string, so the market state is a basis state \(\lvert s_t \rangle\). A transition oracle writes the next-step probabilities into amplitudes, and a sequence of oracles prepares a superposition over every future path. The oracles need no action register, since trades have no market impact. A diagonal observable gives path \(s_T\) the eigenvalue \(\exp(-\lambda R_t^\pi(s_T))\). The value function then follows from its expectation in that superposition.</p> <p>That observable defines a categorical distribution over future returns, which links the method to distributional reinforcement learning. The whole distribution has an exponential support and is impractical to learn. We split the future paths by the Hamming weight of the remaining path and learn one expectation per subset. In a random-walk market, that weight counts the up moves still to come. The model gives the probability of each subset, so the overall expectation follows.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quantum-deep-hedging/distributional_subsets-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quantum-deep-hedging/distributional_subsets-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quantum-deep-hedging/distributional_subsets-1400.webp"/> <img src="/assets/img/blog/quantum-deep-hedging/distributional_subsets.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> A schematic trimodal target (orange). Left: one learned distribution (blue) fitted to its mean misses its shape. Right: three subsets, each with its own learned expectation (green), keep the modes and the tails. </div> <p>One Hamming-weight preserving unitary acts on every subset at once, so a single circuit predicts each subset expectation and the overall one. Two ancilla qubits in \(\lvert 01 \rangle\) give every subset a support of at least two atoms, as the Cramér projection requires. A fixed observable with evenly spaced eigenvalues, independent of the policy, then reproduces the value distribution in expectation.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/blog/quantum-deep-hedging/black_scholes_circuit-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/blog/quantum-deep-hedging/black_scholes_circuit-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blog/quantum-deep-hedging/black_scholes_circuit-1400.webp"/> <img src="/assets/img/blog/quantum-deep-hedging/black_scholes_circuit.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The circuit for the Black-Scholes quantum market. Yellow: t qubits load the past jumps, and Hadamard gates on the next T - t qubits prepare an equal superposition over future jumps. Two ancilla qubits hold 01. Blue: a Brick compound layer on the future and ancilla qubits, with separate angles for each value of each past qubit. Green: measurement of every future and ancilla qubit. </div> <p>Training follows an actor-critic loop. The critic minimizes a squared error between its predicted expectation and the observed \(\exp(-\lambda \widetilde{R}^i_t)\). The expected critic predicts one number per state. The distributional critic predicts it for the Hamming-weight subset the path landed in. The policy then minimizes a one-step utility loss adapted from Murray et al. The loss uses the critic’s value for the next state. With \(O(T^2)\) parameters per network, \(\mathrm{poly}(T)\) training episodes suffice for generalization.</p> <h2 id="results-including-on-hardware">Results, including on hardware</h2> <p>We evaluated the models in three ways: exact classical simulation, the Quantinuum H1-1 noisy emulator, and the H1-1 and H1-2 trapped-ion processors. All training ran in exact simulation.</p> <h3 id="classical-market-orthogonal-layers">Classical market, orthogonal layers</h3> <p>The market follows geometric Brownian motion with zero drift and volatility 0.2. The instrument is a short European call struck at the money, hedged daily over 30 trading days with a proportional transaction cost of 0.01. Each layer has 16 features or 16 qubits. The table gives utilities with costs on 256 paths for classical linear layers and Butterfly orthogonal layers.</p> <table> <thead> <tr> <th>Model</th> <th>Classical utility</th> <th>Butterfly utility</th> <th>Classical parameters</th> <th>Butterfly parameters</th> </tr> </thead> <tbody> <tr> <td>Feed-forward</td> <td>-5.064</td> <td>-5.043</td> <td>881</td> <td>257</td> </tr> <tr> <td>Recurrent</td> <td>-5.075</td> <td>-4.854</td> <td>881</td> <td>257</td> </tr> <tr> <td>LSTM</td> <td>-4.743</td> <td>-4.787</td> <td>569</td> <td>217</td> </tr> <tr> <td>Transformer</td> <td>-4.713</td> <td>-4.822</td> <td>1905</td> <td>865</td> </tr> </tbody> </table> <p>The Butterfly models stay close to their classical counterparts with 29% to 45% of the parameters, and Pyramid layers score about the same. Without costs, every quantum model lands within 0.03 utility of its classical counterpart. We note in the paper that classical pruning might give a similar parameter reduction.</p> <p>On the H1-1 emulator we used one layer per model and 1,000 shots per circuit. The Butterfly LSTM lost the least to noise: -4.809 in simulation, -4.866 on the emulator. On the H1-1 processor we ran the LSTM and the Transformer with 16-qubit Butterfly circuits over 5 days on 4 paths. The LSTM went from -2.176 to -2.194 and its per-path PnL tracked simulation closely. The Transformer fell from -2.195 to -2.539.</p> <h3 id="quantum-market-compound-layers">Quantum market, compound layers</h3> <p>The quantum market is a discretized geometric Brownian motion with one up-or-down jump per day. Maturity is 10 days and the transaction cost is 0.002. Classical simulation of the full Hamming-weight space capped the circuits at 12 qubits. We trained with policy search and with both actor-critic algorithms for 2,000 Adam steps, 3 seeds, and 16 episodes per step.</p> <p>In exact simulation the distributional actor-critic gave the best utility, -3.875 without costs and -4.424 with costs. Policy search gave -4.064 and -4.639, and the expected actor-critic gave -4.193 and -4.668. On the H1-1 emulator, the utilities matched simulation to within 0.02 for all three algorithms, and the distributional policy still scored best.</p> <p>The hardware run used 8 paths on H1-1 and H1-2, with transaction costs, and compared against the Black-Scholes delta hedge.</p> <table> <thead> <tr> <th>Policy</th> <th>Utility, simulation</th> <th>Utility, hardware</th> </tr> </thead> <tbody> <tr> <td>Black-Scholes delta hedge</td> <td>-4.884</td> <td> </td> </tr> <tr> <td>Expected actor-critic</td> <td>-3.547</td> <td>-3.501</td> </tr> <tr> <td>Distributional actor-critic</td> <td>-3.309</td> <td>-3.369</td> </tr> </tbody> </table> <p>Both learned policies beat the delta hedge by more than one utility unit, and the distributional policy was best. Hardware agreed with simulation to within 0.06 in utility. The terminal PnLs were similar path by path.</p> <h2 id="what-this-changes">What this changes</h2> <p>Orthogonal layers slot into standard time-series models and match classical layers with fewer parameters. The LSTM version survives a trapped-ion processor at 16 qubits. Compound layers turn a Hamming-weight preserving circuit into a distributional critic, and that critic gave the best policies in every setting we compared. The circuits are small, since qubit count and depth scale with the maturity.</p> <p>Classical simulation of training capped the maturity at 10 days for compound models. If we trained on the processor with the parameter-shift rule, the maturity could reach a month or more. The model would then be beyond classical simulation. Some circuits we ran are classically simulable, though a simple change of input state makes them hard. A quantum environment for the Heston model is still open. So is a distributional loss with temporal-difference updates.</p>]]></content><author><name></name></author><category term="research"/><category term="quantum-machine-learning"/><category term="finance"/><category term="reinforcement-learning"/><summary type="html"><![CDATA[Quantum neural networks with orthogonal and compound layers act as the policy and the critic in Deep Hedging. A distributional actor-critic gives the best policies, and the models run on a trapped-ion processor with up to 16 qubits.]]></summary></entry></feed>